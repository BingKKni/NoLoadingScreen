# Builds every push and pull request, so a broken commit is caught before anyone
# downloads a jar from it.
name: build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  workflow_call:
    inputs:
      mod_version:
        type: string
        required: false
        default: ''

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: fabric-26.2
            java: '25'
            project: '.'
            artifacts: 'build/libs/'
          - target: fabric-26.3
            java: '25'
            project: 'platforms/fabric-26.3'
            artifacts: 'platforms/fabric-26.3/build/libs/'
          - target: neoforge-1.21.10
            java: '21'
            project: 'platforms/neoforge-1.21.10'
            artifacts: 'platforms/neoforge-1.21.10/build/libs/'
          - target: neoforge-1.21.11
            java: '21'
            project: 'platforms/neoforge-1.21.11'
            artifacts: 'platforms/neoforge-1.21.11/build/libs/'
          - target: fabric-26.1.x
            java: '25'
            project: 'platforms/fabric-26.1'
            family: true
            artifacts: 'platforms/fabric-26.1/build/libs/*-26.1.x*.jar'
          - target: neoforge-26.1.x
            java: '25'
            project: 'platforms/neoforge-26.1'
            family: true
            artifacts: 'platforms/neoforge-26.1/build/libs/*-26.1.x*.jar'
          - target: neoforge-26.2
            java: '25'
            project: 'platforms/neoforge-26.2'
            artifacts: 'platforms/neoforge-26.2/build/libs/'
          - target: forge-26.1.x
            java: '25'
            project: 'platforms/forge-26.1'
            family: true
            artifacts: 'platforms/forge-26.1/build/libs/*-26.1.x*.jar'
          - target: forge-26.2
            java: '25'
            project: 'platforms/forge-26.2'
            artifacts: 'platforms/forge-26.2/build/libs/'
    steps:
      - name: checkout repository
        uses: actions/checkout@v7

      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v6

      - name: setup jdk
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java }}
          distribution: 'microsoft'

      # Caches the Gradle dependency cache, which matters here because Loom pulls
      # down the ~39 MB Minecraft client jar plus its libraries on a cold run.
      - name: setup gradle
        uses: gradle/actions/setup-gradle@v6

      - name: make gradle wrapper executable
        run: chmod +x ./gradlew

      - name: build
        env:
          BUILD_VERSION: ${{ inputs.mod_version }}
        run: |
          args=()
          if [ -n "$BUILD_VERSION" ]; then args+=("-Pversion=$BUILD_VERSION"); fi
          ./gradlew -p ${{ matrix.project }} build check --stacktrace "${args[@]}"

      - name: verify unchanged family jar on every game version
        if: matrix.family
        env:
          BUILD_VERSION: ${{ inputs.mod_version }}
          PROJECT: ${{ matrix.project }}
        run: |
          args=()
          if [ -n "$BUILD_VERSION" ]; then args+=("-Pversion=$BUILD_VERSION"); fi
          jars=("$PROJECT"/build/libs/*-26.1.x.jar)
          test "${#jars[@]}" -eq 1
          jar=$(realpath "${jars[0]}")
          sha256sum "$jar" > "$RUNNER_TEMP/family.sha256"
          for game in 26.1 26.1.1 26.1.2; do
            ./gradlew -p "$PROJECT" check --stacktrace "${args[@]}" \
              "-PtestMinecraftVersion=$game" "-PverificationJar=$jar"
          done
          sha256sum --check "$RUNNER_TEMP/family.sha256"

      - name: capture build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: noloadingscreen-${{ matrix.target }}-dev-build
          path: ${{ matrix.artifacts }}
          if-no-files-found: error
