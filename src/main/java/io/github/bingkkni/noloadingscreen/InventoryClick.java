package io.github.bingkkni.noloadingscreen;

/** Loader-independent vanilla inventory gestures; PlaceholderInteraction owns the local whitelist. */
public enum InventoryClick {
	PICKUP, QUICK_MOVE, SWAP, CLONE, THROW, QUICK_CRAFT, PICKUP_ALL
}
